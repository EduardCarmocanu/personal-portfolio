import { RouterModule, Routes } from '@angular/router';

const APP_ROUTES: Routes = [
	// { path: 'projects/:projectTitle' component: ProjectDetails }
];

export const Routing = RouterModule.forRoot(APP_ROUTES);
